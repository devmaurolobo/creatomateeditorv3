import styled from 'styled-components';
import { Button } from '../Button';

/**
 * StyledButton é um componente estilizado baseado no componente Button.
 * Ele é utilizado no componente CreateButton para exibir ações (por exemplo, 
 * para criar ou finalizar a renderização do vídeo).
 */
export const StyledButton = styled(Button)`
  display: block;
  margin-left: auto;
`; 